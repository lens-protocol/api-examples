query DoesFollow($request: DoesFollowRequest!) {
  doesFollow(request: $request) {
    items {
      followerProfileId
      status {
        value
        isFinalisedOnchain
      }
    }
    pageInfo {
      prev
      next
    }
  }
}
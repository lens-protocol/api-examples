mutation createAttachMediaData($request: PublicMediaRequest!) {
  createAttachMediaData(request: $request) {
    signedUrl
    media {
      item
      type
      cover
      source
      altTag
    }
  }
}

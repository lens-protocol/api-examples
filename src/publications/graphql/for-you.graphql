query ForYou($request: PublicationForYouRequest!) {
  forYou(request: $request) {
    items {
      ... on Post {
        id
      }

      ... on Quote {
        id
      }
    }
    pageInfo {
      prev
      next
    }
  }
}
